# 设计决策与验证记录

| 决策/观察 | 依据 | 状态 |
|---|---|---|
| 使用 M01-M10 稳定模块 ID | CMake 目录和运行时对象边界 | 已确认的组织决策 |
| 以 `muInfo` 作为 D01 | 真实 tools 构建目标，覆盖 init/device query | 已确认入口，运行未验证 |
| 不执行构建/安装验证 | 目标依赖 GPU、私有子模块；安装会写系统目录 | 已执行的安全约束 |
| 记录 `CSV_UNSUPPORTED` 拼写风险 | README 与 CMake 分支名称不同 | 已确认差异；影响待验证 |
| 先写静态调用链，再补动态 trace | 当前只有源码检查，无运行日志 | 当前工作策略 |

## 验证缺口

- `cmake` 配置是否能在目标机完成：未知。
- `muInfo` 的真实 stdout 和设备数量：未知。
- M3D queue submit、memory manager 的硬件副作用：未展开/未验证。
