# D01：修改练习

1. **低风险**：增加一个已有 `MU_DEVICE_ATTRIBUTE_*` 的输出字段，并保持 `checkMuErrors` 处理。
2. **错误处理**：将“属性查询失败”改为记录 device id 后继续其他设备；先确认工具预期是否允许部分结果。
3. **可观测性**：增加 API 阶段标签，不改变 Driver API。
4. **回归**：至少覆盖无设备、单设备和多设备；硬件环境未提供时只能准备测试方案，不能宣称通过。

不要把 D01 改成隐式调用 kernel/内存分配，否则它就不再是最小查询 Demo。
