# 开发实践：测试配方

- 证据状态：测试策略；未执行

## 最小回归矩阵

| 层 | 必测 |
|---|---|
| API | 合法/空指针/非法枚举/非法 ID/重复调用 |
| 生命周期 | 未初始化、重复创建、提前释放、reset 后旧句柄 |
| 异步 | callback/event、超时、错误、资源提前销毁 |
| 跨模块 | ACL→Runtime 错误映射、GE→Runtime 执行、Runtime→HAL |
| ABI | 头文件、导出符号、结构体布局、ioctl 32/64 位 |
| 设备 | 多设备、故障恢复、驱动/固件不匹配（需硬件） |

## 执行声明

先跑各仓 UT，再跑跨仓集成和真实设备测试；在报告中写明命令、环境、实际输出和未执行项，不以文档中的命令代替验证。
