# AG-K09 安全、安保与治理

## 核心问题
如何防止 prompt injection、越权工具、SSRF、数据泄露、成本攻击并保留审计能力。

## 先修与不变量
先修：威胁建模、网络隔离、密钥和访问控制。关键不变量：不可信内容不能提升权限；工具网关强制 allowlist；秘密不进入 prompt/日志；高风险操作可审批和撤销。

## 题目索引
代表题目：AG-A003、AG-E003；完整索引见 `../00-overview/coverage-matrix.md`。

## 实践入口
以网页中的间接注入为输入，验证内容隔离、出口限制、审批和告警链路。
