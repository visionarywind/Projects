# AI Infra 常见误区

- 把 GPU 利用率、单次 benchmark 或平均延迟当成端到端性能结论。
- 把调度成功、进程退出或消息发送成功当成业务成功。
- 只看显存总量，不看碎片、生命周期、NUMA、缓存和临时 workspace。
- 只增加机器或 worker，不分析队列、拓扑、带宽、回压和最慢节点。
- 只保存模型权重，不保存训练恢复所需的 optimizer、数据和随机状态。
- 把未运行的硬件/网络/集群实验写成已验证事实。

