                                                    Accelerating LLM Inference via Dynamic KV Cache
                                                       Placement in Heterogeneous Memory System
                                                                  Yunhua Fang , Rui Xie , Asad Ul Haq , Linsen Ma , Kaoutar El Maghraoui ,
                                                                            Naigang Wang , Meng Wang , Liu Liu , Tong Zhang



                                            Abstract—Large Language Model (LLM) inference is increasingly
                                         constrained by memory bandwidth, with frequent access to the key-
                                         value (KV) cache dominating data movement. While attention sparsity
                                         reduces some memory traffic, the relevance of past tokens varies over
                                         time, requiring the full KV cache to remain accessible and sustaining
                                         pressure on both bandwidth and capacity. With advances in interconnects
arXiv:2508.13231v2 [cs.AR] 15 Sep 2025




                                         such as NVLink and LPDDR5X, modern AI hardware now integrates
                                         high-bandwidth memory (HBM) with high-speed off-package DRAM,
                                         making heterogeneous memory systems a practical solution. This work
                                         investigates dynamic KV cache placement across such systems to maxi-
                                         mize aggregated bandwidth utilization under capacity constraints. Rather   Fig. 1: Illustration of suboptimal (left) vs. improved (right) token
                                         than proposing a specific scheduling policy, we formulate the placement    placement across HBM and off-package DRAM. The row of the
                                         problem mathematically and derive a theoretical upper bound, revealing     table represents the auto-regressive decoding steps, while the column
                                         substantial headroom for runtime optimization. To our knowledge,
                                         this is the first formal treatment of dynamic KV cache scheduling in
                                                                                                                    represents the increasing token index.
                                         heterogeneous memory systems for LLM inference.
                                            Index Terms—LLM inference, data placement, heterogeneous memory
                                         system.
                                                                                                                    bandwidth within an order of magnitude of HBM, while offering
                                                                                                                    significantly greater capacity.
                                                                                                                       In AI infrastructures equipped with heterogeneous memory sys-
                                                                  I. I NTRODUCTION                                  tems, where off-package DRAM offers lower, yet comparable, band-
                                                                                                                    width to HBM, runtime data placement scheduling becomes a critical

                                         T     RANSFORMER-based large language models [1] (LLMs) infer-
                                               ence dominates the resource utilization of modern AI infrastruc-
                                         ture, yet its scaling is hindered by memory bandwidth bottlenecks.
                                                                                                                    performance factor due to the dynamic variation in token importance
                                                                                                                    during LLM inference. Since LLM inference is heavily bandwidth-
                                                                                                                    bound, overall throughput is closely tied to the efficient utilization
                                         These arise in the read-intensive decode stage, where KV cache             of the aggregated bandwidth across the memory hierarchy. In such
                                         accesses predominate. However, the bandwidth of memory subsys-             systems, the placement of frequently accessed tokens significantly
                                         tems has not kept pace with the accelerating compute capabilities          affects performance: allocating important tokens to slower memory
                                         of modern processors [2], resulting in a mismatch that limits the          leads to under-utilization of high-bandwidth resources and degraded
                                         inference throughput. As a result, memory bandwidth has become a           inference speed. This challenge underscores the importance of dy-
                                         fundamental constraint on the performance and scalability of LLM           namic data placement strategies that adapt to evolving attention
                                         inference workloads. Recent research has demonstrated that enforcing       importance, as illustrated in Fig. 1. Yet, despite its growing relevance,
                                         attention sparsity [3] is an effective way to alleviate the memory         data placement scheduling in heterogeneous memory systems remains
                                         bandwidth bottleneck during LLM inference. However, because token          largely unexplored in the context of LLM inference.
                                         importance evolves dynamically during decoding, the system must               To address this gap, this work introduces a formal framework
                                         retain the entire KV cache in memory to ensure any token can               for modeling the data placement problem in heterogeneous memory
                                         be accessed when its relevance resurfaces [4]. This demand places          systems during LLM inference. Instead of proposing a specific
                                         pressure not only on bandwidth but also on memory capacity.                scheduling algorithm, we aim to characterize the design space and
                                            High Bandwidth Memory (HBM) offers the highest throughput               performance potential by deriving a theoretical upper bound through
                                         among existing memory technologies and is well-suited to address           heuristic optimization. Using the classical simulated annealing (SA)
                                         bandwidth challenges. However, its limited capacity, constrained by        algorithm, we construct a dynamic placement strategy that ap-
                                         die stacking and packaging density, as well as its high cost and           proximates the optimal allocation of KV cache entries based on
                                         power consumption, make it undesirable as the sole memory resource         future access patterns. Our simulation results show that this upper
                                         for large-scale LLM inference. Consequently, heterogeneous memory          bound achieves up to 5.87× higher throughput compared to a static
                                         systems are becoming inevitable in future AI infrastructure. Thanks        placement scheme, revealing significant headroom for improvement.
                                         to continuous advances in DRAM interfaces (e.g., LPDDR5X) and              By quantifying the performance gap between this bound and ex-
                                         high-speed serial interconnects (e.g., NVLink, UALink, and PCIe),          isting heuristics, we hope to catalyze future research on adaptive
                                         AI processors such as GPUs and TPUs can readily complement HBM             data placement scheduling strategies to unlock the full potential of
                                         with off-package DRAM fabrics that provide lower, yet increasingly         heterogeneous memory systems in LLM inference.
                                         comparable, bandwidth. For example, the design of NVIDIA Grace
                                         Hopper Superchip [5] shows that off-package DRAM can provide
                                                                                                                                              II. BACKGROUND
                                           Yunhua Fang, Rui Xie, Asad Ul Haq, Linsen Ma, Meng Wang, Liu Liu, and    A. Token Importance Variation
                                         Tong Zhang are with the Rensselaer Polytechnic Institute, Troy, NY 12180
                                         USA.
                                                                                                                       LLM inference comprises prefill and decode stages. In the prefill
                                           Kaoutar El Maghraoui and Naigang Wang are with IBM T.J. Watson           stage, the input prompt is processed in parallel to generate initial
                                         Research Center, Yorktown Heights, NY 10598 USA.                           tokens, while the decode stage generates tokens auto-regressively,
                                                                             As the bandwidth disparity between HBM and external memory
                                                                          reduces, it becomes increasingly beneficial to utilize the aggregate
                                                                          bandwidth of the entire heterogeneous memory system. Efficiently
                                                                          distributing inference-time datasets, such as KV cache entries, across
                                                                          these memory devices can significantly impact system performance,
                                                                          emphasizing the need for intelligent memory management strategies
                                                                          that consider both bandwidth and capacity constraints.

   (a) Token 2777 at layer 8.             (b) Token 4286 at layer 8.                III. DYNAMIC DATA I N -M EMORY P LACEMENT
Fig. 2: The importance variation of 2 tokens during the decode stage.       Despite the growing memory demands of LLM inference, the prob-
                                                                          lem of dynamically placing inference-time datasets, particularly KV
                                                                          cache, across heterogeneous memory systems remains underexplored.
relying on the KV cache to store key and value vectors from               This section introduces a formal framework for formulating the data
prior tokens for efficient attention computation. This read-heavy         placement problem in this context. Rather than proposing a specific
behavior on KV cache in the decode stage places significant pressure      scheduling algorithm, our objective is to characterize the design space
on memory bandwidth and often becomes the dominant inference              by deriving a theoretical upper bound on speed performance. While
bottleneck [6].                                                           not intended as a deployable policy, this upper bound quantifies the
   Recent methods mitigate full KV cache access via dynamic token         performance potential under idealized migration strategies, with the
bypassing, selectively fetching influential tokens for attention. This    hope of inspiring future research on principled and efficient runtime
leverages the unequal contributions of past tokens to the current         data placement for LLM inference in heterogeneous memory systems.
one, reducing bandwidth usage while preserving accuracy. However,
token importance varies over time, with relevance shifting during
                                                                          A. Mathematical Formulation of Inference Latency
the decode stage. To demonstrate, we analyzed attention scores from
LLaMA-3.1-8B [7] on LongBench [8], examining the attention heat-             This subsection presents a mathematical formulation of LLM infer-
maps and selecting two representative tokens that show the attention      ence latency, which serves as the basis for evaluating the performance
variation during the inference at layer 8. Attention scores are used      potential of KV cache placement strategies in heterogeneous memory
as proxies for importance: higher values indicate greater influence on    systems. We focus exclusively on the decode stage, where memory
the current token generation. As shown in Fig. 2, token importance        bandwidth is the primary bottleneck. This work centers on exploiting
fluctuates substantially across decoding steps, with alternating peaks    the variation of tokens’ importance throughout the inference process.
and troughs that reflect evolving contextual dependencies.                Specifically, model weights are accessed consistently across every
   Due to this temporal variability, dynamic token bypassing strategies   inference step, rendering their placement in HBM essential for
must ensure that all KV cache entries remain accessible in memory,        maximizing bandwidth utilization and minimizing latency overheads
even those being determined as not important at the moment, since         associated with these invariant accesses. Furthermore, in architectures
their relevance may increase later. As a result, although bypassing       employing techniques such as Mixture of Experts (MoE), KV cache
reduces bandwidth pressure, it does not reduce memory capacity            assumes an even greater proportional role in the data access, ampli-
requirements. This limitation is becoming increasingly significant: as    fying its impact on performance and justifying targeted scheduling
model sizes grow and techniques such as chain-of-thought prompting        efforts. Hence, the full LLM model resides permanently in HBM and
extend sequence lengths, the total KV cache footprint expands             the only scheduling variable is the placement of KV cache entries
rapidly. Meanwhile, the capacity of high-bandwidth memory tech-           across the memory hierarchy.
nologies such as HBM is improving at a much slower pace. This                We model the decode stage as a sequence of inference steps,
growing disparity underscores the need to reconsider how KV cache         each indexed by a pair (n, l), where n denotes the token being
accessibility is managed in memory systems.                               generated and l represents the transformer layer, which includes both
                                                                          Multi-Head Attention (MHA) and Multi-Layer Perceptron (MLP)
                                                                          submodules. Let t(n,l) represent the latency of step (n, l). The total
B. Heterogeneous Memory Architecture for LLM
                                                                          inference latency T is the cumulative latency across all steps:
   To meet the growing memory demands of LLM inference amid                                                X
limited HBM capacity, a natural architectural direction is to adopt                                  T =        t(n,l) .                      (1)
heterogeneous memory systems that incorporate multiple memory                                              ∀(n,l)

technologies (e.g., HBM, LPDDR, DDR), each with distinct band-               Each step may involve data movement between the processor and
width and cost trade-offs. Recent advances in memory interface            both HBM and off-package DRAM. Let th(n,l) and te(n,l) denote the
speeds and interconnect technologies have significantly increased the     latencies associated with accessing HBM and off-package DRAM,
practical bandwidth of external memory. For example, LPDDR5X              respectively. The step latency is defined as the maximum of the two:
devices can reach speeds of up to 8,533 MT/s, and NVLink 4.0                                                 n              o
supports up to 100 GB/s per link. These improvements have reduced                             t(n,l) = max th(n,l) , te(n,l) .             (2)
the bandwidth gap between HBM and external memory to within
an order of magnitude. A prominent example is NVIDIA’s GH200                 We now define th(n,l) and te(n,l) based on the KV
Grace Hopper Superchip [5], which combines a Hopper GPU with              cache placement and migration at each step. Let S(n,l) =
                                                                              o        i      w        w
96 GB of HBM3 delivering up to 4 TB/s of bandwidth, and a Grace           {M(n,l) , M(n,l) , H(n,l) , E(n,l) } represent the scheduling decision at
CPU connected to 512 GB of LPDDR5X. The GPU and CPU are                   step (n, l):
                                                                                  o
linked via NVLink-C2C, offering up to 900 GB/s of bandwidth. This            • M(n,l) : KV cache data migrated from HBM to off-package
design allows the GPU to directly access CPU-attached LPDDR5X                   DRAM,
                                                                                  i
memory at high speed, effectively extending both capacity and usable         • M(n,l) : KV cache data migrated from off-package DRAM to
memory bandwidth beyond what HBM alone can provide.                             HBM,
       w      w
  • H(n,l) , E(n,l) : newly written KV cache entries to HBM and                     {±1, ±2} while keeping R fixed, (ii) a ratio move ∆R that perturbs
    off-package DRAM, respectively.                                                 the migration ratio by {±0.1} while keeping W fixed, and (iii)
     r
Let H(n,l) denote the data read from HBM for inference, and Bh the                  a diagonal move ∆W ∆R that applies one perturbation of each
HBM bandwidth. The latency of HBM accesses can be expressed as                      kind simultaneously. Because 80% of proposals touch exactly one
                      r        w        i        o
                                                                                    parameter, the algorithm provides a clear attribution of each accepted
                     H(n,l) + H(n,l) + M(n,l) + M(n,l)                              improvement to either W or R; the remaining 20% of diagonal
         th(n,l) =                                     ,                      (3)
                                     Bh                                             moves enable the algorithm to cross ridges where only coordinated
where | · | denotes the volume of data transferred. We assume                       adjustments of W and R reduce latency. For the cooling mechanism,
that off-package DRAM is connected via full-duplex serial links                     we initialize the target initial acceptance ratio (p0 = 0.8) high enough
(e.g., NVLink, UALink), with uni-directional bandwidth Bk , and                     that uphill moves are accepted. The temperature is then reduced
internal DDR/LPDDR DRAM channels providing bandwidth Bd . Let                       with the cooling rate (α = 0.9), so the acceptance probability
  r                                                                                 of worse solutions declines smoothly and the walk shifts from
E(n,l) be the data read from off-package DRAM for inference. The
corresponding access latency is:                                                    exploration to exploitation. The process terminates when the best
                                                                                    latency improves by less than a predefined threshold (0.1%) across
                                                                                    successive temperature levels, when the temperature falls below a
                  r
                 E(n,l)
                                      (    w
                                          E(n,l)    o
                                                 + M(n,l)        i
                                                                M(n,l)              minimal cutoff, or when a fixed iteration budget is reached, ensuring
  te(n,l) =                   + max                         ,            ,
              min{Bk , Bd }                     Bk               Bk                 a balanced trade-off between solution quality and runtime.
                                                                                       Based on the assumption of known attention access patterns, the
                                       w
                                      E(n,l)    i
                                             + M(n,l)    o
                                                      + M(n,l)
                                                                      )
                                                                                    algorithm can place every KV cache entry in the optimal location
                                                                          .   (4)
                                                  Bd                                before the compute phase demands it. The performance it reports
                                                                                    constitutes a theoretical upper bound that no real-time policy, lacking
   Under each scheduling decision S, the placement of KV cache
                 r           r                                                      such foresight, can outperform it. This upper bound analysis quanti-
data affects H(n,l)    and E(n,l)  , and thereby alters the step latencies
 h          e
                                                                                    fies the headroom left by simpler heuristics.
t(n,l) and t(n,l) . As a result, the total inference latency T varies with
KV cache placement.                                                                                           IV. E VALUATION
   Let PH denote the capacity usage percentage of HBM, and assume
                                                                                    A. Methodology
off-package DRAM capacity is sufficiently large to hold all the
KV cache. The goal of placement scheduling is to minimize the                          To evaluate the potential of optimizing KV cache placement for
total inference latency while respecting HBM capacity limits. The                   accelerating LLM inference, we implemented a behavioral simulator
optimization problem is thus formulated as:                                         that models memory accesses during the decode stage of inference
                             X           n                o                         under a heterogeneous memory hierarchy. We assume that inference
                     min            max th(n,l) , te(n,l)                           performance is primarily constrained by memory bandwidth, enabling
                      S
                            ∀(n,l)                                     (5)          us to estimate total inference latency using the formulation provided
                      s.t. PH ≤ 100%                                                in Section III-A. The memory system configuration is based on the
                                                                                    NVIDIA GH200 Grace Hopper Superchip [5], as detailed in Table I.
B. Theoretical Upper Bound Exploration
                                                                                                  TABLE I: Memory system configuration.
   The optimization problem, as defined in Section III-A, encom-
passes a vast solution space, making exhaustive enumeration of all                        Component                Configuration               Value
scheduling decisions at each step infeasible. Therefore, we search for                    HBM                      Bandwidth                 4.9 TB/s
a near-optimal solution to quantify the theoretical upper bound by                                                 Capacity                    24 GB
adopting the Simulated Annealing (SA) [9] algorithm to explore the
solution space. Since we are deducing the theoretical upper bound,                        Off-package DRAM         Link bandwidth           900 GB/s
we assume that the attention access patterns at each step in the decode                                            DRAM bandwidth           500 GB/s
stage are a priori known. However, it is impossible to scan the entire                                             Capacity                  480 GB
future steps’ attention access patterns and decide the placement plan
at each step. Two tunable parameters are exposed to the SA optimizer:                  We base our experiments on the LLaMA-3.1-8B model [7], which
W , the number of future decoding tokens to evaluate, which controls                has a model size of approximately 16 GB, leaving around 8 GB of
the exploration of the vast search space; and R ∈ [0, 1], the ratio of              HBM capacity available for KV cache. To explore scenarios where
actual KV caches migration in all KV caches that can be migrated                    KV cache capacity requirements exceed the available HBM space,
based on evaluation information to control the migration overhead.                  we construct a long-context inference workload. Specifically, we feed
   At each step, the SA algorithm collects the KV caches that will                  the LLaMA-3.1-8B model with prompts of approximately 30k tokens
be accessed in the next W tokens, ranks them based on their                         from the NarrativeQA dataset of the LongBench benchmark [8] and
access frequency in a priority queue, and weighs the corresponding                  allow it to autoregressively decode 10K tokens. We record the layer-
migration overhead by only selecting the top-R portion of all KV                    wise attention scores generated during the decode stage to obtain
caches qualified for migration. Let T (W, R) denotes the objective                  realistic access patterns for our memory-system simulations.
(total decode latency); then ∆T = Tnew − Tcurrent is the cost in-                      For comparison, we evaluate the following three KV cache place-
crease of the proposed neighbour, and C is the current “temperature”                ment strategies:
that monotonically decreases during cooling. The Metropolis rule                       • Unlimited HBM: Assumes unlimited HBM capacity, storing
P (accept) = exp[ −∆T C
                         ] therefore gives the probability of accepting                   all data including model weights and KV cache in HBM for
a worse move, which falls as either ∆T grows or C cools.                                  maximum bandwidth access. This idealized scenario yields
   In each annealing iteration the optimizer samples one of three                         optimal performance but is unrealistic.
proposal operators with probabilities (0.4, 0.4, 0.2), respectively:                   • Static Placement: KV cache entries are written once without
(i) a window move ∆W that perturbs the look-ahead window by                               subsequent migration. New entries fill HBM until capacity is
Fig. 3: The normalized tokens per second of strategies compared to
                      the Static Placement.
                                                                           Fig. 4: Normalized tokens/s vs.            Fig. 5: The HBM hit rates
                                                                                   Unlimited HBM.                     (attention sparsity = 60%).
    reached, after which they are placed in off-package DRAM, with
    no dynamic relocation.
  • Reactive Scheduling: Upon accessing a KV cache entry absent            migrations and optimize aggregate bandwidth. The HBM hit rate is
    from HBM, it is promoted to HBM. If HBM is full, the least             also controlled at an efficient ratio as shown in Fig 5. The wide gap to
    recently used (LRU) entry is evicted to off-package DRAM,              baseline strategies highlights substantial potential for advanced tech-
    adapting placement to observed reuse patterns.                         niques (e.g. predictive modeling, reinforcement learning, or online
  • Page Granularity Scheduling: Emulates the Quest approach [4]           learning of token access patterns) to bridge this divide.
    by managing KV cache at page granularity (page size: 16).
    Entire pages are migrated with perfect foresight of token impor-                                    V. C ONCLUSION
    tance, though this incurs overhead from including unimportant             This work presents the first formal exploration of dynamic KV
    tokens in the same page.                                               cache placement for LLM inference in heterogeneous memory sys-
  • SA-Guided Scheduling: Uses simulated annealing (SA), as                tems. Instead of proposing a specific scheduling policy, we mathe-
    described in Section III-B, with a priori access statistics to         matically formulate the placement problem and derive a theoretical
    optimize KV cache placement. This upper-bound strategy lever-          upper bound using simulated annealing, assuming perfect knowledge
    ages perfect foresight of token importance, highlighting potential     of token importance. Simulation results reveal up to a 5.87× perfor-
    gains over baselines via advanced scheduling.                          mance gap between this upper bound and simple baseline strategies,
                                                                           underscoring the significant headroom for improvement. By making
B. Sensitivity Study                                                       this gap explicit, our study provides a foundation and motivation
                                                                           for future research on practical, adaptive scheduling techniques that
   To assess the impact of key factors on KV cache placement
                                                                           can approach this upper bound and better harness the capabilities of
strategies, we examine sensitivity to attention sparsity and token
                                                                           heterogeneous memory architectures.
importance variation. Attention sparsity refers to the fraction of past
tokens excluded from attention computation at each decoding step;
                                                                                                          R EFERENCES
higher sparsity reduces computation and memory access overhead.
Token importance variation measures shifts in influential tokens           [1] A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. N. Gomez,
                                                                               Ł. Kaiser, and I. Polosukhin, “Attention is all you need,” in Proceedings
across steps: low variation occurs when important tokens remain                of the 31st International Conference on Neural Information Processing
largely consistent between consecutive decodings, while high vari-             Systems, vol. 30, 2017, pp. 5998–6008.
ation indicates substantial changes.                                       [2] A. Gholami, Z. Yao, S. Kim, C. Hooper, M. W. Mahoney, and K. Keutzer,
   Fig. 3 presents normalized tokens per second for the five strategies,       “AI and memory wall,” IEEE Micro, vol. 44, no. 3, pp. 33–39, 2024.
                                                                           [3] Z. Zhang, Y. Sheng, T. Zhou, T. Chen, L. Zheng, R. Cai, Z. Song, Y. Tian,
relative to Static Placement, across varying attention sparsity levels.
                                                                               C. Ré, C. Barrett, Z. A. Wang, and B. Chen, “H2o: Heavy-hitter oracle for
At low sparsity (e.g., below 60%), Reactive Scheduling and Page                efficient generative inference of large language models,” in Proceedings
Granularity Scheduling yield only marginal gains. This stems from              of the 37th International Conference on Neural Information Processing
excessive migration overhead: dense token importance inflates the              Systems, vol. 36, 2023, pp. 34 661–34 710.
working set beyond HBM capacity, triggering frequent migrations            [4] J. Tang, Y. Zhao, K. Zhu, G. Xiao, B. Kasikci, and S. Han, “QUEST:
                                                                               Query-aware sparsity for efficient long-context LLM inference,” in Pro-
that compete with inference accesses and reduce bandwidth effi-                ceedings of the 41st International Conference on Machine Learning, vol.
ciency. In contrast, high sparsity (e.g., above 80%) shrinks the active        235, 21–27 Jul 2024, pp. 47 901–47 911.
KV cache footprint, minimizing migration costs and narrowing the           [5] NVIDIA,       “NVIDIA        Grace     Hopper     Superchip     Architec-
performance gap between SA-Guided Scheduling and other strategies.             ture.” [Online]. Available: https://www.nvidia.com/en-us/data-center/
                                                                               grace-hopper-superchip/
   To evaluate token importance variation, we synthesized two trace
                                                                           [6] A. Agrawal, N. Kedia, A. Panwar, J. Mohan, N. Kwatra, B. Gulavani,
files simulating low and high variation scenarios during LLM in-               A. Tumanov, and R. Ramjee, “Taming Throughput-Latency tradeoff in
ference. Fig. 4 shows normalized tokens per second relative to                 LLM inference with Sarathi-Serve,” in 18th USENIX Symposium on
Unlimited HBM at 60% attention sparsity. Under low variation,                  Operating Systems Design and Implementation, Jul. 2024, pp. 117–134.
adaptive strategies like Page Granularity Scheduling and SA-Guided         [7] Meta-AI, “Llama 3.1: A collection of multilingual large language
                                                                               models,” 2024, accessed 2025-05-19. [Online]. Available: https:
Scheduling approach optimal performance, as stable token impor-                //huggingface.co/meta-llama/Llama-3.1-8B
tance requires minimal migrations for effective bandwidth utilization.     [8] Y. Bai, X. Lv, J. Zhang, H. Lyu, J. Tang, Z. Huang, Z. Du, X. Liu,
Conversely, high variation degrades these strategies due to frequent           A. Zeng, L. Hou, Y. Dong, J. Tang, and J. Li, “LongBench: A bilingual,
migrations needed to track shifting access patterns. Overall, dynamic          multitask benchmark for long context understanding,” in Proceedings of
                                                                               the 62nd Annual Meeting of the Association for Computational Linguistics
in-memory placement offers greater benefits for inference tasks with           (Volume 1: Long Papers), Aug. 2024, pp. 3119–3137.
low importance variation.                                                  [9] S. Kirkpatrick, C. D. Gelatt Jr., and M. P. Vecchi, “Optimization by
   Across all scenarios, SA-Guided Scheduling consistently outper-             simulated annealing,” Science, vol. 220, no. 4598, pp. 671–680, May
forms (4× to 5×) the baseline by leveraging foresight to minimize              1983.
